# 本機開發環境行為準則

## 資料庫連線限制
1. 本機電腦為「開發用電腦」，無法且不應嘗試連接或啟動本地的 PostgreSQL 服務 (5432 Port)。
2. PIMCP 專案具有 "In-Memory" 降級機制，可以在無資料庫狀態下執行.
3. WTCA App 亦不可強求本機資料庫連線，啟動失敗時請修改為跳過資料庫連線或使用記憶體模擬。
4. **絕對不要**提出在 Docker 中啟動 PostgreSQL 或安裝 PostgreSQL 服務的建議。

## 背景服務重新啟動限制
5. **絕對不要**在背景自動重新啟動 PIMCP (Port 3011) 與 WTCA App (Port 3003) 的背景服務以載入最新網頁資源。本機是開發用電腦，這些服務的啟動與重啟應交由開發者在終端機 (Terminal) 中手動執行，以避免連接埠 (Port) 衝突。
